import React from 'react';
import { BrowserRouter as Router, Routes, /* Add Route once there are paths*/ } from 'react-router-dom';

function App() {
  return (
      <Router>
        <Routes>
        </Routes>
      </Router>
  );
}

export default App;